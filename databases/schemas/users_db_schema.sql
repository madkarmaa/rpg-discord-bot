/* Schema for  `users` database */
/* https://www.sqlitetutorial.net/ */

/*
To merge a branch into the master branch in Visual Studio Code, follow these steps:

Open your project in Visual Studio Code and make sure you have the Git plugin installed.

In the Source Control panel, which is usually located on the left side of the screen, click the "Branches" button.
This will open a list of all the branches in your project.

In the "Branches" list, select the branch you want to merge into the master branch.
This will display the changes you've made on that branch in the diff view.

To merge the branch into the master branch, click the "Merge" button, which is located at the top of the "Branches" list.
This will open a dialog where you can confirm the merge.

In the merge dialog, select the "master" branch as the target branch and click "Merge".
This will merge the selected branch into the master branch.

If there are any conflicts between the two branches, Git will alert you and you will need
to resolve those conflicts manually before the merge can be completed. You can use the "Resolve conflicts" button
in the Source Control panel to resolve conflicts.
*/

BEGIN TRANSACTION;

COMMIT TRANSACTION;